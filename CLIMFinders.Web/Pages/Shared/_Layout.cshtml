<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Impound Car Finders</title>

    <!-- SEO Meta Tags -->
    <meta name="description" content="Find impounded cars quickly and easily." />
    <meta name="keywords" content="impound, car finder, vehicle search" />
    <meta name="format-detection" content="telephone=no" />
    <meta name="HandheldFriendly" content="true" />    
    <link href="@Url.Content("~/css/master.css")" rel="stylesheet" />

    <style>
        /* Full height layout */
        html, body {
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        /* Main content should grow to fill space */
        .main-content {
            flex: 1;
        }

        /* Footer at the bottom */
        .footer {
            width: 100%;
            text-align: center;
            padding: 10px 0; 
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" asp-area="" asp-page="/Index"><img src="~/images/logo.png"></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-page="/Index">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-page="/Privacy">Privacy</a>
                        </li>
                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item">
                                <a class="nav-link text-dark" href="/Business/ManageVehicles">Vehicle Listing</a>
                            </li>
                        }
                    </ul>
                    <ul class="navbar-nav">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-dark" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="fa fa-cog" aria-hidden="true"></i> <b>@User?.Identity?.Name</b>
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li><a class="dropdown-item" asp-area="" asp-page="/MyAccount"><i class="fas fa-user me-2"></i>My Account</a></li>
                                    <li><a class="dropdown-item" asp-area="" asp-page="/ChangePassword"> <i class="fas fa-key me-2"></i>Change Password</a></li>
                                    <li><a class="dropdown-item" asp-area="" asp-page="/Logout"> <i class="fas fa-power-off me-2"></i>Logout</a></li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-area="" asp-page="/Subscription">Register</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-area="" asp-page="/Login">Login</a>
                            </li>
                        }                        
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <partial name="_ValidationScriptsPartial" />
   <main class="container my-4">
        @RenderBody()
    </main>
    
    <footer class="footer mt-auto">
        <div class="container">
            <p>&copy; @DateTime.Now.Year Impound Car Finders. All rights reserved.</p>
        </div>
    </footer>
    <!-- Include validation scripts -->
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
